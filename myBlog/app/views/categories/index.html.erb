<%= render 'shared/title', title: 'All Category'%>


<div class="btn btn-primary">
<%= link_to 'new category', new_category_path, { :style=>'color: white; font-size: 16px', :class => "css_class"} %>
</div>

<div>
<br> <!-- TODO: Fix this -->
</div>

<% @categories.reverse.each do |category| %>
<div class="row">
  <div class="well col-md-6">
    <h5><%= link_to category.title, category_path(category) %></h5>
  </div>
</div>
<% end %>